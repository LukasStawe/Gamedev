using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "New Bundle", menuName = "Inventory/Item/Bundle")]
public class ScriptableItemBundle : ScriptableItem
{

    public ScriptableItem item;
    public int amount;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
